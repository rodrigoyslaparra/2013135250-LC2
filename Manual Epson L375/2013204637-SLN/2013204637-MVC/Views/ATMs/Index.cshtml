@model IEnumerable<_2013204637.ATM>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescATM)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescATM)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ATMId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ATMId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ATMId })
        </td>
    </tr>
}

</table>
